#include <stdlib.h>
#include <mcu_fatal.h>
#include <mcu_macros.h>

int readl(char line[]){
while(1){
    fatal (i==MAXLINE, "Erreur: ligne trop longue \n", -1 );
    char c = getchar();

    fatal(i+1 == )
    if (c=='\n'){
        line[i] = '\n';
        line[i+1] = '\0';
        return i+1; 
    }
    if (c ==EOF){
        line[i] = '\0';
        return EOF;
    }
}
}